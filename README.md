# wlw noise detector

This is the firmware for the arduino board with the following functions:

* Measure environmental noise
* Send measurement values via WiFi to the elixir 'noise' app
* Show last 8 values on a led matrix
* Sound the 110db 'shut up' horn when a level is reached
